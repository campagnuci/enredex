# Database
DATABASE_URL=postgresql://enredex:enredex@localhost:5432/enredex

# API
NODE_ENV=development
HOST=0.0.0.0
PORT=3001
JWT_SECRET=change-me-to-a-long-random-string
ACCESS_TOKEN_TTL=15m
REFRESH_TOKEN_TTL_DAYS=30

# Frontend URL (CORS origin + email links)
WEB_URL=http://localhost:5173

# Email: leave SMTP_URL unset to log emails to the console in dev
# SMTP_URL=smtp://user:pass@smtp.example.com:587
EMAIL_FROM=Enredex <no-reply@enredex.local>
